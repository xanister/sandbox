{
  "name": "sandbox",
  "private": true,
  "scripts": {
    "start": "node -e \"console.log('sandbox repo has no runtime')\"",
    "dev": "node -e \"console.log('sandbox repo has no dev server')\"",
    "stop": "node -e \"console.log('sandbox repo has nothing to stop')\""
  }
}
